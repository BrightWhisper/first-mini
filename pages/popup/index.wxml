<view class="recharge-btn">
  //listenBuy是监听点击购买事件
  <popup showModal="{{showModal}}" content="{{bottomModalContent}}" />
  <text bind:tap="showPop">立即购买</text>
</view>


<view bind:tap="blinker">
再点两下
</view>